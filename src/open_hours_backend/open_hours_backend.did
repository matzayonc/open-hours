service : {
  open_at : (blob) -> (vec record { nat8; vec principal }) query;
  open_counts : () -> (nat64, vec vec nat16) query;
  open_for : (opt principal) -> (blob) query;
  set_open_hours : (blob) -> ();
}
